# MiniRack8 Environment Configuration
# SECURITY: This file contains sensitive credentials - never commit to version control
#
# NOTE: The installer (install.sh) will auto-generate secure passwords during setup.
# You can override any value below by setting environment variables before running install.sh.
#
# Example:
#   PIHOLE_WEBPASSWORD=my-custom-password sudo bash install.sh --profile homelab

# =============================================================================
# Pi-hole
# =============================================================================
PIHOLE_WEBPASSWORD=changeme
PIHOLE_IP=192.168.1.2

# =============================================================================
# Nextcloud / MariaDB
# =============================================================================
MYSQL_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=changeme

# =============================================================================
# MinIO
# =============================================================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme

# =============================================================================
# InfluxDB
# =============================================================================
INFLUXDB_PASSWORD=changeme

# =============================================================================
# Drone CI
# =============================================================================
DRONE_GITEA_SERVER=http://gitea:3000
DRONE_GITEA_CLIENT_ID=
DRONE_GITEA_CLIENT_SECRET=
DRONE_RPC_SECRET=
DRONE_SERVER_HOST=drone.local
DRONE_SERVER_PROTO=http

# =============================================================================
# WireGuard
# =============================================================================
WG_PRIVATE_KEY=
WG_PUBLIC_KEY=
WG_PEERS=3
